package OOPS;

import static OOPS.a.Message.message;

// the above import message is the method which is public we can access anywhere in the folder if the method is public else we can't.

public class Greeting {
    public static void main(String[] args) {
        message();
    }
}
